import { componentMap } from "@/components/mdx/mdxComponents";

import "@/app/globals.css";

export default function App({ Component, pageProps }) {
  return <Component {...pageProps} components={componentMap} />;
}
